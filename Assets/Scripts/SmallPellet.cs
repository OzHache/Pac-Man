using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SmallPellet : Consumable
{
    // Start is called before the first frame update
    void Start()
    {
        ScoreValue = 10;
        //todo: Apply to all small pellets
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
